/* styling */
require('styles/main.scss');
/* js */
import $ from 'jquery';
import { log, logTitle } from 'logger';
/* your imports */
import * as Math from './Math.js';
logTitle('Title');
/* coding examples */

log(Math.add(2,2))
log(Math.sub(2,2))
log(Math.pi)